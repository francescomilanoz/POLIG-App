using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShowArrow : MonoBehaviour
{
    void Start()
    {


       // GameObject.Find("AttachedArrow").GetComponent<GameObject>().SetActive(false);

    }

    // Update is called once per frame
    void Update()
    {

        /*
        GameObject vars = GameObject.Find("PublicVariables");
        PublicVariablesScript script = vars.GetComponent<PublicVariablesScript>();
        
        if (script.roomCodeScanned)
        {
         //   GameObject.Find("AttachedArrow").GetComponent<GameObject>().SetActive(true);
        } else
        {

            GameObject self = gameObject.GetComponent<GameObject>();
            Destroy(self);
        }
        
        if (script.scanResult.Equals("arrow"))
        {
            GameObject.Find("Arrow").GetComponent<Renderer>().enabled = true;
            GameObject.Find("Video").GetComponent<Renderer>().enabled = false;
        }
        else if (script.scanResult.Equals("null"))
        {
            GameObject.Find("Arrow").GetComponent<Renderer>().enabled = false;
            GameObject.Find("Video").GetComponent<Renderer>().enabled = false;
        }
        */
        
    }
}
