using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ArrowManagerTMP : MonoBehaviour
{
    //GameObject obj;
    //Load3DArrow script;

    // Start is called before the first frame update
    void Start()
    {
        //obj = GameObject.Find("buttonRow");
        //script = obj.GetComponent<Load3DArrow>();
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
        INIParser ini = new INIParser();
        ini.Open(Application.persistentDataPath + "map.txt");

        string color = ini.ReadValue("ArrowSettings", "color", "blue");

        Quaternion qa = new Quaternion(90, 180, 37, 0);

        if (color.Equals("green"))
        {
            GameObject arrow = GetComponent<GameObject>().GetComponentInChildren<GameObject>(); ;
                arrow.transform.SetPositionAndRotation(arrow.transform.position, qa);
        }
          */  
    }
}
