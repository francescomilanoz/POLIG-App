using System.Collections;
using System.Collections.Generic;
using UnityEngine;




public class PublicVariablesScript : MonoBehaviour
{
    //public string scanResult;
    //public bool artworkScanned;
    //public bool roomCodeScanned;

    // Start is called before the first frame update
    void Start()
    {
        //scanResult = "null";
        //artworkScanned = false;
        //roomCodeScanned = false;
    }

    // Update is called once per frame
    void Update()
    {
        //Debug.Log("Artwork: " + artworkScanned + "Room: " + roomCodeScanned);
    }
}
